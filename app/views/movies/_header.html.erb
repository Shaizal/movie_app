<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Movie App', root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'New Movie', new_movie_path, class: "nav-link" %>
        </li>
      </ul>
      <div class="d-flex">
        <% if user_signed_in? %>
          <% unless current_user.phone_number_verified? %>
            <%= link_to 'Verify Phone Number', phone_number_form_path, class: "btn btn-info btn-sm mx-2" %>
          <% end %>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: "btn btn-success btn-sm mx-2" %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-primary btn-sm mx-2" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

