<h1>Movies in <%= @genre.genre %> Genre</h1>
<ul>
  <% @movies.each do |movie| %>
    <li><%= link_to movie.title, movie_path(movie) %></li>
  <% end %>
</ul>

<%= link_to 'Back to Movies', movies_path %>